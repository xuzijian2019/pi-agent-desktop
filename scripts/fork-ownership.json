{
  "schemaVersion": 1,
  "upstreamBaseline": "v0.8.2",
  "note": "Single source of truth for the pi-web merge boundary. `driftedUpstreamFiles` lists files agegr/pi-web owns that this fork has also edited — every one is a potential merge conflict, and a potential SILENT semantic conflict when git merges cleanly but the two sides changed different things. The automated sync (.github/workflows/component-updates.yml) intersects each incoming upstream release with this map to decide whether it may push to main unattended. Human-facing rationale lives in docs/ownership-boundaries.md.",
  "ignored": [
    "package-lock.json",
    "bun.lock",
    "src-tauri/Cargo.lock"
  ],
  "riskModel": {
    "basis": "structuralDrift",
    "thresholds": {
      "high": 250,
      "medium": 30
    },
    "note": "Risk follows STRUCTURAL drift, not total drift. Replacing an upstream inline style object with a className produces a large diff, but an upstream edit to the same line conflicts textually and fails the sync loudly — that is safe. Rewritten JSX trees and code moved across files are what merge cleanly while being wrong. components/SkillsConfig.tsx is the reference: 56 changed lines, only 14 structural, correctly rated low. Recompute with the hunk classifier described in docs/ownership-boundaries.md after any significant change to a shared file."
  },
  "autoPushPolicy": {
    "blockOnRisk": [
      "high",
      "medium"
    ],
    "note": "Risk levels that force the sync to open a reviewed PR instead of pushing straight to main and triggering a signed release. Most upstream releases will land here, because the remaining drift is accepted by decision rather than pending cleanup — see `dispositions` and `decisionLog`. Narrow this list only if the drift itself actually goes away."
  },
  "driftedUpstreamFiles": {
    "components/SessionSidebar.tsx": {
      "risk": "high",
      "drift": 2133,
      "reason": "~500 lines were MOVED OUT into components/ProjectPicker.tsx and components/path-ui.tsx, which do not exist upstream. git merge does not track cross-file moves: an upstream edit to the project-picker block can merge back cleanly and silently resurrect a second copy.",
      "structuralDrift": 1478,
      "disposition": "accepted"
    },
    "components/AppShell.tsx": {
      "risk": "high",
      "drift": 1843,
      "reason": "Top-bar redesign (toolbar consolidated into a more-menu, inline styles to classNames) plus the mount point for desktop chrome. The desktop-only parts moved to components/desktop/; the rest is the fork's own interaction design, not desktop plumbing, so this file stays high risk by design rather than by neglect. Header/More cleanup (ui-refresh decisions 7 and 8) DELETES upstream UI rather than adding fork UI — the Full history toolbar button, the Session stats more-item and the session top panel — which raises structural drift by 37 lines, because upstream still ships all of it. Numbers are the measured values from `npm run drift` against the recorded baseline. Saved-task append and chip conflict fixes bring measured structural drift to 1411. Phase 4 adds an import, one local element and one prop to fill ChatWindow's empty-state slot: 3 structural lines.",
      "structuralDrift": 1411,
      "disposition": "accepted"
    },
    "components/FileViewer.tsx": {
      "risk": "high",
      "drift": 599,
      "reason": "Adds FileViewerToolbar and FileViewerStatus, file icons, a live-sync indicator and a retry action. Both components are defined inside FileViewer.tsx rather than moved out, so there is no cross-file resurrection risk — only a large diff. Kept deliberately.",
      "structuralDrift": 430,
      "disposition": "accepted"
    },
    "components/FileExplorer.tsx": {
      "risk": "high",
      "drift": 904,
      "reason": "Adds selection highlighting (selectedPath/onSelectPath) and moves git status colours onto CSS variables. Generic improvement, no desktop coupling. Kept deliberately.",
      "structuralDrift": 731,
      "disposition": "accepted"
    },
    "components/ModelsConfig.tsx": {
      "risk": "medium",
      "drift": 281,
      "reason": "Already the correct shape: upstream inline style objects swapped for native-* classNames, 14 structural lines out of 148. Conflicts here are textual and trivially resolvable. Use this file as the model for restyling a shared component.",
      "structuralDrift": 113,
      "disposition": "accepted"
    },
    "components/TabBar.tsx": {
      "risk": "medium",
      "drift": 116,
      "reason": "Accessibility work: role=\"tab\", aria-selected, roving tabIndex, arrow-key navigation, Enter/Space activation, plus an empty state. A generic improvement with no desktop coupling, so it cannot be extracted anywhere — the only ways to remove this drift are upstreaming it or deleting the accessibility support. Kept deliberately.",
      "structuralDrift": 61,
      "disposition": "accepted"
    },
    "components/ChatWindow.tsx": {
      "risk": "high",
      "drift": 950,
      "reason": "Branding copy and a redesigned empty state. Part of the fork's interaction design. Kept deliberately. Workbench integration adds 32 structural lines relative to d0a8be2; totals conservatively add that delta because the v0.8.2 baseline is unavailable locally. Transcript search adds read-only historical rendering and exact-match expansion; the added drift is a conservative upper bound for this integration. Phase 4 adds one optional emptyStateSlot ReactNode prop and renders it inside .chat-empty-state: 3 structural lines, and the empty-state UI itself lives in components/workbench/NewTaskGuide.tsx.",
      "structuralDrift": 857,
      "disposition": "accepted"
    },
    "hooks/useTheme.ts": {
      "risk": "medium",
      "drift": 135,
      "reason": "Follows the OS colour scheme until the user picks a theme explicitly, and drops the ToggleOrigin argument so the view transition always starts from the centre. The view transition itself is unchanged from upstream. Kept deliberately.",
      "structuralDrift": 135,
      "disposition": "accepted"
    },
    "components/PluginsConfig.tsx": {
      "risk": "medium",
      "drift": 193,
      "reason": "Already the correct shape: 6 structural lines out of 54, the rest is style-to-className substitution. Nothing to revert.",
      "structuralDrift": 105,
      "disposition": "accepted"
    },
    "components/ChatInput.tsx": {
      "risk": "high",
      "drift": 1844,
      "reason": "Composer chrome restyle. Phase 3 stream H2 and Phase 4 part C together replaced 134 of the 136 inline style objects with classes in app/native-theme.css; the 2 that remain carry a measured popover position. Structural drift is 1044 against 1002 on main: +5 from the part C checkpoint (the per-tone banner colour constant and the queued-message recall button's pointer handlers), +3 from diff realignment across the remaining conversions, and +34 from dropping the onMouseEnter/onMouseLeave colour hacks on the composer controls, which the classifier scores as structural because it deletes upstream lines. That last 34 is isolated in a single commit. Saved-draft restoration adds 4 structural lines, bringing the current measured structural drift to 1015. Upstream changed this file in v0.8.1, and components/ChatInput*.test.mjs plus tests/e2e/workbench.spec.ts cover it.",
      "structuralDrift": 1015,
      "disposition": "accepted"
    },
    "components/MessageView.tsx": {
      "risk": "medium",
      "drift": 737,
      "reason": "Message bubble markup adjusted. Transcript search adds read-only historical rendering and exact-match expansion. Phase 3 stream H3 then replaced ~90 inline style objects with classes in app/native-theme.css, which is why the total is large and almost two thirds cosmetic; the structural half is unchanged transcript-search and written-file rendering.",
      "structuralDrift": 201,
      "disposition": "accepted"
    },
    "components/SkillsConfig.tsx": {
      "risk": "low",
      "drift": 56,
      "reason": "Already the correct shape: 3 structural lines out of 44, the rest is style-to-className substitution. Nothing to revert.",
      "structuralDrift": 14,
      "disposition": "accepted"
    },
    "lib/i18n/registry.ts": {
      "risk": "low",
      "drift": 4,
      "structuralDrift": 4,
      "disposition": "accepted",
      "reason": "Relative imports of ./messages/en and ./messages/zh-CN carry explicit .ts extensions so lib/i18n/registry.test.mjs can load the file through Node's native type stripping (same constraint that put allowImportingTsExtensions in tsconfig.json). Two import lines; conflicts here would be textual."
    },
    "next.config.ts": {
      "risk": "medium",
      "drift": 21,
      "reason": "Desktop standalone build branch. Also holds serverExternalPackages, which upstream updates whenever it adds a pi package — do NOT auto-derive that list here.",
      "disposition": "accepted"
    },
    "app/layout.tsx": {
      "risk": "low",
      "drift": 24,
      "reason": "native-theme.css import, branding title, dark-mode bootstrap script. Workbench integration adds 1 structural lines relative to d0a8be2; totals conservatively add that delta because the v0.8.2 baseline is unavailable locally.",
      "structuralDrift": 24,
      "disposition": "accepted"
    },
    "eslint.config.mjs": {
      "risk": "low",
      "drift": 7,
      "reason": "Adds ignores for the desktop build output directories.",
      "disposition": "accepted"
    },
    "lib/rpc-manager.ts": {
      "risk": "medium",
      "drift": 234,
      "reason": "Small fork edit in a file upstream changes often; watch it despite the low drift count. Workbench integration adds 80 structural lines relative to d0a8be2; totals conservatively add that delta because the v0.8.2 baseline is unavailable locally.",
      "structuralDrift": 234,
      "disposition": "accepted"
    },
    "components/FileIcons.tsx": {
      "risk": "low",
      "drift": 5,
      "reason": "Adds an icon size parameter, consumed by the fork's FileViewer toolbar.",
      "structuralDrift": 3,
      "disposition": "accepted"
    },
    "components/BranchNavigator.tsx": {
      "risk": "low",
      "drift": 5,
      "reason": "className hook only, no structural change — the intended shape for every restyled file.",
      "structuralDrift": 0,
      "disposition": "accepted"
    },
    "package.json": {
      "risk": "low",
      "drift": 13,
      "reason": "Desktop scripts and Tauri deps. Name stays @agegr/pi-web on purpose — see docs/ownership-boundaries.md.",
      "disposition": "accepted"
    },
    "tsconfig.json": {
      "risk": "low",
      "drift": 5,
      "reason": "resolveJsonModule for branding, plus allowImportingTsExtensions: lib/*.test.mjs load .ts sources through Node's native type stripping, which requires explicit extensions on relative imports.",
      "disposition": "accepted"
    },
    ".gitignore": {
      "risk": "low",
      "drift": 7,
      "reason": "Desktop build output plus local agent tooling state (.claude/, .spec-workflow/).",
      "disposition": "accepted"
    },
    "README.md": {
      "risk": "low",
      "drift": 264,
      "reason": "Fork-specific product docs, English and the language switcher's default entry point. Conflicts here are noisy but harmless — resolve by keeping the fork version.",
      "disposition": "accepted"
    },
    "README.zh-CN.md": {
      "risk": "low",
      "drift": 264,
      "reason": "Fork-specific product docs, Simplified Chinese translation of README.md. Same handling as README.md. Upstream's other translations (ja, ru) are deleted in this fork on purpose — if a sync resurrects them, delete them again rather than translating Pi Agent docs into a language nobody here maintains.",
      "disposition": "accepted"
    },
    "lib/file-access.ts": {
      "risk": "low",
      "drift": 13,
      "structuralDrift": 13,
      "disposition": "accepted",
      "reason": "Routes homedir() through userHome() so Next's file tracer stops folding it into a build-time glob of the whole user profile — the walk that made every Windows build fail. One import plus call swaps; conflicts here would be textual."
    },
    "app/api/default-cwd/route.ts": {
      "risk": "low",
      "drift": 6,
      "structuralDrift": 6,
      "disposition": "accepted",
      "reason": "Routes homedir() through userHome() so Next's file tracer stops folding it into a build-time glob of the whole user profile — the walk that made every Windows build fail. One import plus call swaps; conflicts here would be textual."
    },
    "lib/directory-browser.ts": {
      "risk": "low",
      "drift": 25,
      "structuralDrift": 25,
      "disposition": "accepted",
      "reason": "Routes homedir() through userHome() so Next's file tracer stops folding it into a build-time glob of the whole user profile — the walk that made every Windows build fail. One import plus call swaps; conflicts here would be textual."
    },
    "lib/skill-lock.ts": {
      "risk": "low",
      "drift": 4,
      "structuralDrift": 4,
      "disposition": "accepted",
      "reason": "Routes homedir() through userHome() so Next's file tracer stops folding it into a build-time glob of the whole user profile — the walk that made every Windows build fail. One import plus call swaps; conflicts here would be textual."
    },
    "app/api/cwd/validate/route.ts": {
      "risk": "low",
      "drift": 28,
      "structuralDrift": 28,
      "disposition": "accepted",
      "reason": "Routes homedir() through userHome() so Next's file tracer stops folding it into a build-time glob of the whole user profile — the walk that made every Windows build fail. One import plus call swaps; conflicts here would be textual."
    }
  },
  "forkOwnedPaths": [
    "src-tauri/",
    "desktop/",
    "scripts/",
    ".github/workflows/",
    "app/native-theme.css",
    "app/api/updates/",
    "components/desktop/",
    "components/ProjectPicker.tsx",
    "components/UpdateReminder.tsx",
    "components/AppSettings.tsx",
    "components/path-ui.tsx",
    "components/MissingFolderNotice.tsx",
    "components/fork-extractions.test.mjs",
    "lib/missing-folder.ts",
    "lib/branding.ts",
    "lib/branding.test.mjs",
    "lib/app-update-types.ts",
    "lib/app-updates.ts",
    "lib/app-updates.test.mjs",
    "lib/desktop-updater.ts",
    "lib/desktop-window.ts",
    "docs/ownership-boundaries.md",
    "components/workbench/",
    "app/workbench.css",
    "app/api/agent/activity/",
    "app/api/git/branches/",
    "app/api/saved-tasks/",
    "app/api/sessions/[id]/outputs/",
    "lib/activity-types.ts",
    "lib/activity.ts",
    "lib/checkout-guard.ts",
    "lib/git-branch-types.ts",
    "lib/git-branches.ts",
    "lib/i18n/messages/workbench.ts",
    "lib/output-types.ts",
    "lib/composer-popover.ts",
    "lib/panel-modes.ts",
    "lib/prepare-outgoing.ts",
    "lib/saved-tasks.ts",
    "lib/session-forks.ts",
    "lib/session-outputs.ts",
    "lib/task-types.ts",
    "lib/web-ui-client.ts",
    "lib/web-ui-client.test.mjs",
    "lib/git-inventory.ts",
    "lib/web-ui-route.ts",
    "lib/web-ui-store.ts",
    "lib/workbench.test.mjs",
    "lib/transcript-search.ts",
    "lib/transcript-search.test.mjs",
    "lib/transcript-types.ts",
    "app/api/transcript-search/"
  ],
  "dispositions": {
    "accepted": "Reviewed and kept. The drift is either the fork's own interaction design or a generic improvement with no desktop coupling, so it cannot be extracted into components/desktop/. Removing it would mean deleting a feature. These files stay permanent review points; that is the running cost of a UI fork, not a backlog item. Do not reopen without new information.",
    "reduce": "Drift that can be removed without losing anything — desktop-only code that belongs in components/desktop/, or fork additions sitting in an upstream file that can move to a fork-owned one. Act on these."
  },
  "decisionLog": [
    "2026-07-26 — lib/api-types.ts: fork update types moved to lib/app-update-types.ts; file now matches upstream and left the manifest.",
    "2026-07-26 — components/AppShell.tsx: desktop window chrome moved to components/desktop/ (-53 structural). The remaining 376 lines are the top-bar redesign; accepted.",
    "2026-07-26 — TabBar, FileExplorer, useTheme, FileViewer: assessed as generic improvements with zero desktop coupling. Upstreaming was declined, so accepted rather than reverted — reverting would delete working features to save CI review time.",
    "2026-07-26 — lib/file-access.ts and app/api/default-cwd/route.ts: a literal homedir() in readdirSync/join arguments made @vercel/nft glob the whole user profile at build time, which is why the Windows release leg never succeeded. Routed both through defaultCwdBase(); accepted as new drift in two upstream files.",
    "2026-07-26 — lib/user-home.ts added; five upstream files now call userHome() instead of homedir(). A literal homedir() reaching an fs call made @vercel/nft glob the entire user profile at build time. lib/directory-browser.ts (added upstream in v0.8.1) was the source of the fatal <home>/**/* walk, which is why Windows releases started failing exactly when v0.8.1 merged.",
    "2026-07-28 — merged pi-web v0.8.2 (i18n, explorer quick changes viewer, project trust / restricted mode, Shift+Delete). Fork markup kept, upstream i18n strings adopted inside it. The upstream CWD picker and PiWebTitle scramble blocks that merged back into SessionSidebar.tsx were dropped again; the collapsible-explorer chevron stayed out (fork's explorer is always open) while the changes-viewer toggle was kept. Numbers refreshed against the v0.8.2 baseline.",
    "2026-07-28 — lib/i18n/registry.ts (new upstream file in v0.8.2): added .ts extensions to its two relative imports so its test loads under Node type stripping; accepted as new low drift.",
    "2026-07-31 — READMEs reduced to English (README.md, the default) plus Simplified Chinese (README.zh-CN.md), matching the two locales the UI actually ships in lib/i18n. README.ja.md and README.ru.md were still upstream pi-web docs describing an npx web server, so they were deleted rather than kept as stale descriptions of a product this fork no longer is. A sync that restores them should delete them again.",
    "2026-09-19 — missing-folder path: a project whose directory was deleted offered an enabled + and a composer where every cwd-scoped call 403s. The logic lives in the new fork-owned lib/missing-folder.ts and components/MissingFolderNotice.tsx; AppShell.tsx and SessionSidebar.tsx gained only a handle prop, a mount point and two guards. Accepted as a small addition to two already-high-risk files, with erosion sentinels in components/fork-extractions.test.mjs.",
    "2026-09-19 — network hygiene: idle /api/ polling coalesced into lib/web-ui-client.ts and a shared per-cwd poller (lib/git-inventory.ts). Two upstream files took guard lines only, no restructuring: FileExplorer.tsx stops the watch-dir EventSource reconnect loop once the folder answers 403/404 (+18 structural, still medium), SessionSidebar.tsx skips its 10 s worktree poll for a cwd that is gone (+7 structural, still high). Both accepted — the polls they guard are upstream's own.",
    "2026-09-19 — one git entry point (decision 6): the sidebar's two git branch switchers were deleted — the \"Switch branch\" list inside the worktree dropdown and the project context-menu branch submenu. Both checked out branches straight from the sidebar, duplicating the composer chip components/workbench/BranchControl.tsx, which is now the only git branch entry point. SessionSidebar.tsx drops 260 structural lines (1808 -> 1548) and keeps only the new-worktree datalist's branch fetch; the dead state, handlers, .sidebar-project-context-menu-branch-* CSS and seven sidebar.* i18n keys went with them. Decision 5 in the same stream renames the header's conversation-fork navigator to \"Forks\" and hides it when the session has no fork point; the predicate lives in the new fork-owned lib/session-forks.ts (unit-tested) and AppShell.tsx took one import plus one conditional, +3 structural. Net for the fork: -263 structural lines.",
    "2026-09-19 — header/More cleanup (ui-refresh decisions 7 and 8): the Full history toolbar button and the Session stats More item plus its \"session\" top panel were removed from components/AppShell.tsx. The HTML export keeps a single entry point as an Export HTML item in the same More menu, reusing /api/sessions/:id/export?inline=1 and the openExternal path Tauri needs for inline viewing. components/SessionStatsPanel.tsx (fork-only, never listed in this manifest) is deleted; SessionStatsInfo already lived in lib/pi-types.ts, so no type had to move. AppShell contextUsage state fed only that panel and went with it; sessionStats stays for the composer context ring and auto-naming. Both removals delete UPSTREAM features, so a clean merge could resurrect either silently — components/fork-extractions.test.mjs gains a REMOVED_UPSTREAM_UI sentinel for exactly that. Accepted.",
    "2026-09-19 — panel modes 8 -> 4 (Files, Activity, Search, Saved Tasks). Browser was deleted outright; Diff and Outputs became collapsible Changes and Pinned sections inside Files (components/workbench/ChangesSection.tsx, PinnedSection.tsx); Context became a composer chip (components/workbench/SendPreview.tsx) mounted through the existing BranchControl slot. components/AppShell.tsx gained +6 structural lines net (590 -> 596). components/ChatInput.tsx and components/ChatWindow.tsx are unchanged in line count: the dead onOpenContext prop was renamed to sendPreview in place, three swapped lines each and no additions, so neither file's recorded drift moves. Note that `npm run drift` already exited 1 on a clean tree before this stream — six risk labels disagree with the classifier because the recorded numbers predate the Phase 1 merges. Refreshing them flips components/ModelsConfig.tsx off `low`, which scripts/fork-ownership.test.mjs asserts by name, so that re-rating is left to a deliberate manifest pass rather than smuggled into this stream.",
    "2026-09-19 — manifest refreshed against the real v0.8.2 baseline (pi-web-upstream remote now fetched). Earlier numbers for ChatInput, ChatWindow, FileExplorer, MessageView, ModelsConfig and PluginsConfig were estimates relative to d0a8be2 and undercounted: measured structural drift is 1051 / 859 / 768 / 254 / 120 / 108. Risk labels follow the thresholds as before; no disposition changed. SkillsConfig.tsx (42 cosmetic / 14 structural) replaces ModelsConfig.tsx as the style-only reference in scripts/fork-ownership.test.mjs.",
    "2026-09-19 — stream H1: components/SessionSidebar.tsx lost 67 of its 70 inline style={{…}} objects. The menus, popovers, worktree dropdown, new-worktree form, headers, session rows and the session actions menu now carry classes defined in the reserved H1 block of app/native-theme.css; the three survivors are the runtime top/left of the two portals and the project path hint. Two onMouseEnter/onMouseLeave colour hacks became :hover rules. Measured drift moved 1987/439/1548 -> 2133/572/1561 (total/cosmetic/structural); risk stays high and the disposition stays accepted. The +13 structural is a measurement artefact, not new divergence: every one of the net-gained lines is a deletion of a BASELINE inline-style tail that the per-line classifier in scripts/measure-fork-drift.mjs cannot see as cosmetic — bare `}}` closers plus boxSizing / overflowWrap / paddingRight, which are absent from its STYLE_PROPS list. Adding those three property names to the list was measured and rejected here: it only recovers 3 of the 13 and it flips components/MessageView.tsx from high to medium, which is a manifest-wide re-rating and not this stream's call. No JSX was restructured, no code moved across files, and no behaviour, aria attribute or test id changed.",
    "2026-09-19 — inline styles → classes, MessageView.tsx (phase 3 stream H3). 97 inline style objects became 7, all of them carrying a derived value (throughput-band colour, diff row background, diff marker colour, patch line tint, the clamp height that the overflow measurement also reads). Everything else moved to a .msg-* section at the end of app/native-theme.css, referencing --radius-*, --text-*, --bg-* and --ease-native so stream G's retuned token values apply without touching this file again. Two-state styling became modifier classes or data-role attributes (tool card data-status, diff rows data-kind/data-side, extension card data-collapsed, action rows data-visible), and eight onMouseEnter/onMouseLeave colour pokes became :hover rules. Cosmetic drift rises 169 → 485; structural drift falls 254 → 252, because the style objects the fork itself had added (the written-file preview, the unified diff row, the collapsible user text) collapsed into classes. Risk stays high. No JSX tree, prop, aria attribute, test id or behaviour changed.",
    "2026-09-19 — inline styles to classes, composer (Phase 3 stream H2). components/ChatInput.tsx went from 136 inline style={{}} objects to 28 and from 38 raw borderRadius literals to 0 (17 inline radii remain, all tokens); the 108 converted ones became classes in the reserved H2 block of app/native-theme.css (completion popovers reuse .native-popover, radii use --radius-sm/md/lg/pill). The 28 that stay carry measured positions, per-tone banner colours or isNarrow/isCompact branches. Deleting a multi-line style object also deletes its closing `}}` line, which the classifier in scripts/measure-fork-drift.mjs scores as structural, so the conversion was budgeted block by block against the fork-added lines it removes: total drift 1378 -> 1549, cosmetic 327 -> 498, structural 1051 -> 1051 (unchanged). Four queued-message style objects and the @-file/effort/tools panels were left inline for that reason, not on design grounds. components/ContextUsageRing.tsx (fork-owned) lost its last inline style and two hover handlers at the same time. Accepted.",
    "2026-09-19 — drift classifier: a multi-line style={{ … }} object now counts as cosmetic as a whole (inner lines and the closing }}), per diff side. Deleting an inline style object used to score its tail as structural, which held back stream H2 in ChatInput.tsx. Re-measured: SessionSidebar 1561→1503, ChatInput 1051→1002, MessageView 252→201 (now medium), AppShell 1487→1405. No code changed.",
    "2026-09-19 — new-task empty state (Phase 4 part 1). A new task showed a composer in a blank area; it now carries one dim guidance line and, when the saved-task library has any, up to three chips ordered by updatedAt plus an \"All saved tasks\" link into the existing panel. All of the UI is fork-owned: components/workbench/NewTaskGuide.tsx, a .new-task-* block at the end of app/workbench.css and three keys in lib/i18n/messages/workbench.ts (en + zh-CN). The two upstream files pay 3 structural lines each and nothing more: ChatWindow.tsx gains one optional emptyStateSlot ReactNode rendered inside .chat-empty-state above the composer (947/854 -> 950/857), AppShell.tsx an import, a local element next to sendPreview and the prop that passes it (1824/1405 -> 1827/1408). A slot was chosen over building the guide inside ChatWindow precisely to keep it at that size, and reusing AppShell's existing useSavedTask callback means the chip and the Saved Tasks panel apply a task through one code path. Both files stay high risk and accepted. The component needs no forkOwnedPaths entry: components/workbench/ already covers the directory.",
    "2026-09-19 — ui-refresh phase 4 (parts A, B, C). A: the Files tab now puts the project tree left of the viewer, by CSS order rather than by moving JSX. B: the Saved Tasks and Activity panels hide their filter rows until there is something to filter. C: the composer conversion finished — components/ChatInput.tsx went from 28 inline style={{}} objects to 2 (the model popover and the project-path tooltip, both measured positions), with the new classes appended to the reserved H2 block of app/native-theme.css. The effort and tools menus lost their max-height IIFEs to `max-height: max(120px, 60dvh)`, which removes fork-added JS. Measured: ChatInput.tsx 1840 total / 796 cosmetic / 1044 structural, AppShell.tsx 1824 / 419 / 1405 (unchanged). ChatInput's structural drift is 42 over the 1002 it carries on main and the cap is not reachable from this branch: the part C checkpoint was already at 1007, the remaining style-to-class work costs +3 in diff realignment, and replacing the control hover handlers with :hover costs +34 because every deleted upstream handler line scores structural. The :hover change is a standalone commit; reverting it returns the file to 1010. Accepted with that trade-off recorded rather than silently split.",
    "2026-09-19 — drift classifier: onMouseEnter/onMouseLeave handlers and e.currentTarget.style pokes are cosmetic; replacing them with :hover rules is the same restyle as swapping an inline style for a className. Re-measured after Phase 4: ChatInput 1044→1011, SessionSidebar 1503→1478, AppShell 1405→1395. No code changed."
  ]
}
