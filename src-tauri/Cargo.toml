[package]
name = "pi-agent-desktop"
version = "0.4.7"
description = "Desktop shell for Pi Agent"
authors = ["pi-agent-desktop contributors"]
license = "MIT"
edition = "2021"
rust-version = "1.85.0"

[lib]
name = "pi_agent_desktop_lib"
crate-type = ["staticlib", "cdylib", "lib"]

[build-dependencies]
tauri-build = { version = "2.5.5", features = [] }

[dependencies]
dunce = "1"
getrandom = "0.3"
libc = "0.2"
serde_json = "1.0.151"
tauri = { version = "2.10.2", features = ["tray-icon"] }
tauri-plugin-clipboard-manager = "^2"
tauri-plugin-dialog = "^2"
tauri-plugin-notification = "^2"
tauri-plugin-process = "^2"
tauri-plugin-updater = "^2"

[target.'cfg(target_os = "linux")'.dependencies]
# Avoid libappindicator's GTK menu bridge, which can render blank labels under GNOME.
ksni = "0.2.2"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[profile.release]
panic = "abort"
codegen-units = 1
lto = "thin"
opt-level = "s"
strip = true
